import React from 'react';
import { shallow } from 'enzyme';
import { App } from '../App';

beforeEach(() => {
});

afterEach(() => {
});

it('renders without crashing', () => {
  shallow(<App />);
});
